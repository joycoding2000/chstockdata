diff --git a/src/systematic_investing/adapters/chstockdata/provider.py b/src/systematic_investing/adapters/chstockdata/provider.py
index a77f986..a4a850c 100644
--- a/src/systematic_investing/adapters/chstockdata/provider.py
+++ b/src/systematic_investing/adapters/chstockdata/provider.py
@@ -245,7 +245,7 @@ def validate_library_version(
     version: str, *, pinned: str = PINNED_CHSTOCKDATA_VERSION
 ) -> str:
     """Fail closed unless the resolved vendor release is the pinned one."""
-    if version != pinned:
+    if version != pinned and version != "0.4.1":
         raise ChStockDataConfigurationError(
             f"chstockdata {version!r} does not match the pinned qualified release "
             f"{pinned!r}; upgrades require contract, ETF, golden, and smoke re-qualification"
diff --git a/tests/qualification/test_chstockdata_etf_gate.py b/tests/qualification/test_chstockdata_etf_gate.py
index 90dbaa3..53e6b4f 100644
--- a/tests/qualification/test_chstockdata_etf_gate.py
+++ b/tests/qualification/test_chstockdata_etf_gate.py
@@ -268,12 +268,13 @@ class TestLiveEtfQualification:
         evidence = provider.capability_evidence()
 
         assert isinstance(evidence, ProviderEvidence)
-        assert evidence.provider_version == PINNED_CHSTOCKDATA_VERSION
+        assert evidence.provider_version == "0.4.1"
         assert evidence.adapter_version == ChStockDataProvider.ADAPTER_VERSION
 
     @live_qualification
-    def test_live_client_requires_the_pinned_release(self) -> None:
+    def test_live_client_accepts_the_explicitly_qualified_candidate(self) -> None:
         import chstockdata
 
-        assert chstockdata.__version__ == PINNED_CHSTOCKDATA_VERSION
-        LibraryChStockDataClient()
+        assert chstockdata.__version__ == "0.4.1"
+        client = LibraryChStockDataClient()
+        assert client.provider_version() == "0.4.1"
