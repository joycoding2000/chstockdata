# RH3-A.2 — Historical Calendar Coverage Closure

**日期：** 2026-09-23  
**历史日历覆盖项：PASS / CLOSED**  
**阶段：Owner Review**

本轮只修复隔离资格环境的数据准备。`chstockdata` 运行时代码、公开 API、消费者生产 pin、冻结 M0/M1 evidence 与 RH3-A.1 审计结论均未修改。RH3-A.1 仍按原报告记为 `BLOCKED`；本报告只证明其后发现的历史日历覆盖项现已满足指定的 2015-01-05 至 2022-12-30 资格要求。没有创建 tag，也没有执行 RH3-B。

## 1. 诊断基线与根因

读取了现存 RH3-A、RH3-A.1 审计、`trading_calendar.py`、`vipdoc_history.py`、消费者真实 `LibraryChStockDataClient` / `ChStockDataProvider`、资格与契约测试。任务所列 `docs/releases/v0.4.0/version-design.md` 在当前 `HEAD` 与工作树中均不存在；日历设计核对使用现存 `docs/releases/v0.4.0/phase3-trading-calendar-audit.md`。

原输入 `C:\Users\Administrator\.chstockdata\cache\vipdoc\sh\lday\sh000001.day` 存在、32 字节记录对齐、可正常解析：279,264 字节，SHA-256 `b80e7ce933deac639f80408899cfdd6d0357f1f176a982822c177b6d85b77a6b`；8,727 个无重复日期，覆盖 1990-12-19 至 2026-09-16。目标区间内有 1,947 个指数交易日，首尾正是 2015-01-05 / 2022-12-30。该区间的指数日期与 `510300`、`518880`、`511010` 三只 ETF 完全一致；`159915` 少一日且没有指数日期以外的日线。ETF 缺一条记录不用于推断全市场休市。原指数与 Sina 的重叠段也没有日期分歧。

以 2026-09-23 和配置阈值 5 天运行的三源实测：

| 来源 | Attempt status | 日期范围 / 记录数 | stale / partial / limitations |
| --- | --- | --- | --- |
| `tdx_vipdoc:index_bars` | `success` | 1990-12-19—2026-09-16 / 8,727 | `stale=true`（末 bar 落后 7 天）；`partial=false`；本地过期限制 |
| `mootdx:index` | `failed_network` | 无 / 无 | 当前隔离 venv 缺少可选 `mootdx`，实际异常为 `ModuleNotFoundError`；探针没有伪造记录 |
| `sina:index_bars` | `success` | 2018-06-29—2026-09-22 / 2,000 | `stale=false`；`partial=false`；无限制 |

原结构化路由的真实 attempts 依次为 `success`、`failed_network`、`success`，最终 `providers_used=[sina]`、`final_provider=sina`、来源 `sina_sh000001`。`mootdx` / Sina 在线请求都固定为 2,000 根；路由在本地过期时仅比较最后 bar 日期，因 Sina 更新至 2026-09-22，便选取其较新的短窗口，丢弃本地可用的更长历史。消费者随后因来源覆盖起于 2018-06-29，对 2015–2022 请求返回 `PARTIAL_RESULT`。**判定为“本地历史完整但被在线窗口替换”的路由覆盖选择缺陷，伴随本地库存过期；并非本地历史缺失或目标段实际缺口。** 当前 `mootdx` 可选依赖缺失不改变此判定。

## 2. 最小修复与来源身份

没有改变冻结的 Calendar 路由选择规则或引入跨源合并。只用包内既有 `refresh_vipdoc` 工具，从既有通达信官方 `https://data.tdx.com.cn/vipdoc/hsjday.zip` 在隔离目录刷新资格输入。HEAD 报告 `Last-Modified: Tue, 22 Sep 2026 07:56:47 GMT`、550,465,240 字节；下载归档 SHA-256 为 `72bf25fb6dd5fbdb1a83c64a37d8ad7c4d9652f7036919b6694ba955b643e968`。未改用户原缓存。可复核的最小资格输入副本位于 `C:\Users\Administrator\.codex\visualizations\2026\09\23\01a0cce6-e76c-7221-9ae5-3bf8da3aa6ae\vipdoc-rh3a2`，仅保留指数及四只 ETF 的 `.day` 文件；副本与官方刷新树中的对应文件逐个 SHA-256 相同。

| 输入文件 | 刷新后 SHA-256 |
| --- | --- |
| `sh/lday/sh000001.day` | `7eeedb6c1691304f21636a88af84730107074b8072c5fb265b8f4098ea28da0c` |
| `sh/lday/sh510300.day` | `56db5fb84ac0965428748b6fa6228622d7e86021e37e2ecc4b0f79d1e88743ab` |
| `sz/lday/sz159915.day` | `caf336760dfe6317f5117f88eee05e366e1bb970459eeb7f1fd53999c38ae9a3` |
| `sh/lday/sh518880.day` | `f73b02ec305007cef07cc77ae08b49c7b583cdad3c3a71c4f3e6f127f2201eef` |
| `sh/lday/sh511010.day` | `3f4225663778f898ef36a8562f4d8774b1a20eae08ea9d1caf7d8d3a5548a78d` |

新指数文件有 8,731 个无重复交易日，覆盖 1990-12-19 至 2026-09-22。旧文件全部日期均保留，仅新增 2026-09-17、18、21、22 四日。新旧指数与四只 ETF 在 2015–2022 的解析后完整日线帧逐项相同。由此隔离输入只改变当前新鲜度，没有改变冻结研究区间的日线语义。

## 3. 修复后路由与真实消费者结果

刷新后的结构化探针：本地 `success`、8,731 条、`stale=false`、`partial=false`、无限制；`mootdx` 仍为上述可选依赖失败；Sina 仍为 2,000 条短窗口。实际路由仅调用本地来源，`attempts=[tdx_vipdoc:success]`、`providers_used=[tdx_vipdoc]`、`final_provider=tdx_vipdoc`，来源 `vipdoc_sh000001`，覆盖 1990-12-19 至 2026-09-22，`stale=false`、`partial=false`。在线探针数据没有被记为最终贡献；也没有把未知日期推断成休市或用 ETF 缺行推断全市场状态。

在两个隔离消费者副本与独立 Python 3.12.14 venv 中，通过真实 `LibraryChStockDataClient` / `ChStockDataProvider.fetch_calendar()` 请求 `XSHG` 的 2015-01-05 至 2022-12-30：

| 版本 | 修复前 | 修复后 | 修复后来源 |
| --- | --- | --- | --- |
| 冻结 0.3.0 | `PARTIAL_RESULT`，零记录 | success；2,917 个自然日、1,947 个开市日 | `chstockdata.load_trading_calendar (vipdoc_sh000001)` |
| 原候选 0.4.0 wheel | `PARTIAL_RESULT`，零记录 | success；2,917 个自然日、1,947 个开市日 | 同上 |

两版逐日 `(session_date, is_open)` SHA-256 均为 `5b6b3a3c36d0c352d009df91119440ca48086577bbb7f8a3835722d8b5b4b39f`。消费者只在供应方明确覆盖的整个请求区间内生成会话；原先缺覆盖时仍返回 `PARTIAL_RESULT`，没有把 unknown 当 closed。候选 wheel 未重建，现存 wheel SHA-256 仍为 RH3-A 审计的 `7b7c3b75c95876c0afff6da8c496e9a077229a2088b939b1a8e42502313f2458`。

## 4. 回归门禁与剩余限制

隔离消费者副本从 RH3-A.1 的冻结 0.3.0 / 限定候选 0.4.0 资格环境复制，补齐独立 Git 元数据以解析消费者冻结提交 `84fd54fae0740bc06b1d250afa4578be29556956`。首次无 `.git` 的复制品使研究 CLI 的 `git rev-parse HEAD` 测试在两版同样失败；补齐后全仓 `pytest -q` 两版均为 **929 passed、5 skipped**，包含 M0/M1 Golden、PIT、parity、snapshot/replay、provider 与 adapter 契约。没有改测试预期或冻结报告。使用最终五文件输入副本单独重跑真实 live 日历/历史覆盖项，两版均 `1 passed`。

五项真实 live 资格组在两版均为 **4 passed、1 failed**，重复一次结果相同。通过项包含路由/OHLCV、单位/日历/历史覆盖、版本证据与版本 guard；唯一失败为 `test_live_tradability_adjustment_and_freshness` 的 Eastmoney 停牌快照请求 `ConnectionError`，消费者收到 `UNAVAILABLE_OR_RATE_LIMITED`。这是本轮外部数据源运行状态，未把失败当空结果或归因于日历修复；因此本报告不宣称整个五项 live 门禁通过，也不宣称 RH3-A 发布门禁开放。

原始完整刷新树 `D:\Dev\Projects\chstockdata\.rh3a2-vipdoc` 仍在工作区，属于本轮生成的隔离数据。自动审批审查拒绝了递归清理，理由是其中含数万份历史行情文件，删除范围超出其判定的授权范围并有不可逆损失风险；未绕过该拒绝。Owner 可审核后决定保留或授权清理。原用户缓存、消费者 main 与其它 worktree 均未修改。

**结论：** 指定的真实历史日历覆盖资格现已通过，RH3-A.2 历史日历项关闭。路由在本地库存再次过期时仍可能选取较新的 2,000 根在线窗口；这是尚未消除的运行条件，不应把本次隔离数据准备描述为通用 Calendar 路由修复。若要改变冻结路由策略、引入权威日历源或跨源合并，需另立设计决策并由 Owner Review。RH3-A.1 保持 `BLOCKED`，生产升级与 RH3-B 均未获本报告授权。
