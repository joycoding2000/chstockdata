# chstockdata v0.4.0 — RH3-A Final Close-out

**日期：** 2026-09-23

**阶段：** Owner Review

**最终结论：** RH3-A 资格门禁通过；本结论不授权创建 tag、上传 PyPI 或创建 GitHub Release。

## 最终状态

| 门禁 | 状态 | 依据 |
| --- | --- | --- |
| Package Qualification | PASS | RH3-A 已审计 wheel/sdist、确定性测试、RH1、compile、build/install、extras 与入口脚本证据均关联到候选 wheel SHA。 |
| TradingAgents | 有限兼容性 PASS | 真实候选与 0.3.0 baseline 结果一致；保留七项 baseline failures 和四项排除测试。 |
| systematic-investing-os | 真实消费者资格认证通过 | 使用已安装 0.4.0 wheel 的真实 adapter；RH3-A.4 五项 live qualification 全部通过。 |
| 历史日历 | PASS，有条件 | 依赖本地已验证 vipdoc 输入仍可读且满足 freshness 阈值；不是通用在线历史覆盖承诺。 |
| Eastmoney 停牌快照 | PASS，有条件 | 开发机和隔离阿里云主机无代理直连并取得完整快照；消费者 live qualification 通过。 |
| Live Qualification | 5 passed | RH3-A.4 在独立消费者资格环境运行五项真实用例；另有 12 项非 live 用例未纳入该组。 |
| 已知历史停牌正向样本 | SUSPENDED | XSHG:688432 于 2026-08-31 经真实消费者 adapter 识别为 SUSPENDED，并与 Eastmoney、BaoStock 和上交所公告吻合。 |

上述 PASS 只建立 v0.4.0 候选的包及消费者资格证据。两个消费者的生产 pin 均保持原样，消费者升级另需批准；RH3-B 发布动作另需 Owner 明确授权。

## 1. 候选身份与审计归档

- 已审计候选提交：771961e29b842666b0e1dd361858e3d665462c95。
- 版本：pyproject 与运行时版本均为 0.4.0。
- 本地候选 wheel：chstockdata-0.4.0-py3-none-any.whl，280,502 bytes，SHA-256 为 7B7C3B75C95876C0AFFF6DA8C496E9A077229A2088B939B1A8E42502313F2458。
- 本地候选 sdist：chstockdata-0.4.0.tar.gz，395,372 bytes，SHA-256 为 16E22C897A8CA91332F4C92ADC8C7F402F802F2915779F1E4C8129BA106C1692。
- 候选之后 main 的后续提交均为文档增量；在 d12ea8b 后，44e910e3 又合入历史日历报告与两份追溯设计文档。当前 main / origin/main 为 44e910e3ee5c0087477bfcee004da2bb93e6bd1e，候选至该提交的树差异仍只有文档，没有运行时代码、依赖、打包配置或发布 workflow 变化。

五份原始阶段报告均已定位：

1. docs/audits/2026-09-23-v0.4.0-rh3a-closeout.md
2. docs/audits/2026-09-23-v0.4.0-rh3a1-systematic-requalification.md
3. docs/audits/2026-09-23-v0.4.0-rh3a2-calendar-closure.md
4. docs/audits/2026-09-23-v0.4.0-rh3a3-eastmoney-live-qualification.md
5. docs/audits/2026-09-23-v0.4.0-rh3a4-direct-egress-and-source-investigation.md

RH3-A.2 原始报告此前未出现在远端 main。本轮在本地找到后，现已由 44e910e3 合入 main 的同一规范路径；报告内容和所列输入哈希与 RH3-A.3 / RH3-A.4 交叉吻合。原始报告未被修改。.rh3a2-vipdoc 未删除，仍作为本地未跟踪证据保留，不纳入源码或发布包。

## 2. 历史审计状态保留

本报告综合后续证据，不改写五份原始报告中的历史结论：

| 报告 | 原始结论 | 历史结论保留 |
| --- | --- | --- |
| RH3-A | BLOCKED | 当时 systematic-investing-os 仍被精确 0.3.0 guard 阻挡。 |
| RH3-A.1 | BLOCKED | 当时指定历史日历窗口确实未覆盖；两版本均返回 PARTIAL_RESULT。 |
| RH3-A.2 | 历史日历 PASS / CLOSED | 历史日历输入修复后覆盖通过；该次五项 live 组仍有 1 项 Eastmoney 连接失败。 |
| RH3-A.3 | BLOCKED | 当时两个真实 HTTP 尝试均未取得响应，不能将其解释为无停牌记录或能力通过。 |
| RH3-A.4 | PASS candidate，Owner Review | 更换为可直连的隔离资格环境后，取得完整快照且五项 live qualification 通过；仍未批准生产 pin 或发布。 |

RH3-A FINAL PASS 是对已追加证据的最终综合判定，不会把早期 BLOCKED 文件改成 PASS。

## 3. Package Qualification

RH3-A 对应的完整身份测试证据与本地候选 SHA 一致：

- deterministic pytest：657 passed、15 deselected。
- RH1 static gate：PASS，24 structured paths；287 项 Ruff 旧债务保留为 baseline，没有新增行 findings。
- compileall：PASS。
- build 与 twine check：PASS；原始构建环境记录为 build 1.6.1、setuptools 84.0.0、twine 7.0.0。
- clean wheel 安装：实际 import 来自隔离 site-packages；distribution metadata 与 runtime 均为 0.4.0，pip check 通过。
- clean sdist 安装：pip check 与 release-version gate 通过。
- Release version consistency gate：以 v0.4.0 运行通过；错误 tag、错误版本及 source-checkout 负向门禁也通过。
- Console scripts：chstockdata-mcp 和 chstockdata-refresh-vipdoc 均从 checkout 外运行 --help 成功。
- core、mootdx、baostock、mcp 独立安装 profiles：均通过安装、pip check 和 import validation。
- mootdx+mcp 因 httpx 约束冲突继续列为不支持组合。

上述测试来自 RH3-A 的候选身份记录；后续 main 只增加文档，没有改变被测 runtime 或 build configuration。

## 4. TradingAgents 有限兼容性

真实刷新后的 TradingAgents 隔离快照分别使用 0.3.0 baseline 和原始 0.4.0 候选 wheel。两者选定确定性组结果完全相同：123 passed、7 failed、4 deselected。

七项失败由一项既有 adjusted-bars source-label assertion 和六项新出现的 Phase 2 projection validation tests 组成；在 0.3.0 baseline 上同样失败，不是 0.4.0 回归。四项刻意依赖 Sina live fallback 的 calendar tests 被排除。生产依赖范围仍为 chstockdata[baostock]>=0.2.0,<0.3；本轮未调整 TradingAgents 的依赖声明或生产 pin。

因此结论限于“无候选差异的兼容性 PASS”，不得写成 TradingAgents 全套测试全绿、已接纳 0.4.0 或已升级生产依赖。

## 5. systematic-investing-os 真实消费者资格

RH3-A.1 的两个隔离环境已针对真实 consumer adapter、M0/M1 contracts、Golden、PIT、snapshot/replay 和 ETF 历史日线比较真实安装包。全仓测试两版均为 929 passed、5 skipped。候选 wheel 身份与本报告列出的 wheel SHA 相同；候选资格版仅在隔离副本内作精确版本资格调整，生产 PINNED_CHSTOCKDATA_VERSION 仍为 0.3.0。

RH3-A.1 的历史日历失败并未被隐藏：当时两版本的真实请求均因输入源窗口较短而返回 PARTIAL_RESULT。RH3-A.2 后来通过正确准备本地 TDX vipdoc 输入关闭该覆盖项。RH3-A.2 / RH3-A.3 的 Eastmoney live 失败也保留在各自报告中。RH3-A.4 随后在无代理直连环境中，用同一候选 wheel 对隔离的真实消费者运行五项 live qualification，结果 5 passed、12 deselected；检查包括真实 provider 路由、OHLCV / pre_close、单位与日历覆盖、停牌与 corporate actions、freshness、ProviderEvidence 和版本 guard。

历史正向样本另用消费者 adapter 验证：XSHG:688432 在 2026-08-31 返回 SUSPENDED；同日 Eastmoney 快照、BaoStock tradestatus=0 与上交所停牌公告相符。该股票样本不证明 BaoStock 对四只 ETF 有完整历史停牌覆盖，也不代表本包已实现 BaoStock fallback。

systematic-investing-os 的 third_party/SOURCES.yaml 仍指向冻结的 0.3.0 commit 143eb5a61e11003a4b29461eeb78deec0ab8d8e5，真实生产 adapter 仍保持精确版本保护。本轮只认证候选，不批准 pin 或 M0/M1 证据更新。

## 6. 历史日历的运行前提

RH3-A.2 使用通达信官方归档输入，归档 SHA-256 为 72bf25fb6dd5fbdb1a83c64a37d8ad7c4d9652f7036919b6694ba955b643e968，源 Last-Modified 为 Tue, 22 Sep 2026 07:56:47 GMT。当前保留目录 .rh3a2-vipdoc 中 sh/lday/sh000001.day 的 SHA-256 为 7eeedb6c1691304f21636a88af84730107074b8072c5fb265b8f4098ea28da0c。

本轮只读复核解析到 8,731 个指数 bar 日期，覆盖 1990-12-19 至 2026-09-22。vipdoc_history_status 显示 history enabled、最新 bar age_days=1、max_staleness_days=5、stale=false。目标 2015-01-05 至 2022-12-30 区间内有 1,947 个指数交易日；RH3-A.2 报告记录两版逐日会话摘要一致。

此 PASS 有输入和时效前提：消费者资格运行必须指向该已验证本地 vipdoc 或字节相同的副本，并满足配置的 freshness 门槛。若本地目录缺失、不可解析或过期，冻结路由可能转用固定 2,000 根在线窗口，不能保证完整历史日历。本轮没有修改 runtime 路由或据此宣称通用历史日历 fallback 已修复。

## 7. Eastmoney、容灾范围与消费者生产状态

RH3-A.4 在开发机及独立隔离云环境中移除代理环境变量后，直接取得 Eastmoney 2026-09-11 完整全市场快照。消费者 qualification 环境安装的 wheel SHA 与本地候选完全相同。四只目标 ETF 的当日 tradability 均为 TRADABLE，快照完整性证据无 warnings；完整五项 consumer live 组通过。

独立断源容灾、多源 failover 设计与 qualification 规划给后续版本，不纳入 v0.4.0。当前证据不支持切换或承诺 BaoStock 停牌 fallback。

两个消费者的生产状态保持原样：

- systematic-investing-os：冻结 source commit 与 0.3.0 版本 guard 不变。
- TradingAgents：现有 baostock extra 依赖范围不变。

任何下游升级须单独批准。历史 M0/M1 evidence、Golden 预期与研究/回测语义未因候选资格通过而改变。

## 8. 范围与非目标

v0.4.0 交付六个 additive structured entry points 和限定的 factual/derived capabilities；legacy API 保持兼容。本轮不声称：

- BaoStock 停牌 fallback 已实现；
- 完整 historical PIT snapshot database 已实现；
- 完整 IPO 或 listing lifecycle eligibility 已实现；
- 所有 legacy API / 全部 structured migration 已完成；
- BSE delisting coverage 已实现；
- mootdx+mcp 组合受支持。

退市 effective-date guard 不等于完整 PIT，部分日历的缺失日期不证明闭市。上述限制仍以发布文档中的说明为准。

## 最终 RH3-A 状态

| 字段 | 状态 |
| --- | --- |
| RH3_A_FINAL_VERDICT | PASS |
| PACKAGE_QUALIFICATION | PASS |
| CONSUMER_QUALIFICATION | PASS (bounded to recorded consumer matrices) |
| PUBLISH_AUTHORIZED | NO |
| TAG_CREATED | NO |
| PYPI_UPLOAD | NO |

RH3-A 收口完成。RH3-B 和生产升级授权仍在 Owner Review。
